import {SafeAreaView, Text} from "react-native";

export default function CreateJobScreen()
{
    return(
        <SafeAreaView>
            <Text style={{color:"#FFF"}}> Create Job Screen</Text>
        </SafeAreaView>
    );
}
import {SafeAreaView, Text} from "react-native";

export default function CreateCourseScreen()
{
    return(
        <SafeAreaView>
            <Text style={{color:"#FFF"}}>Create Course Screen</Text>
        </SafeAreaView>
    );
}
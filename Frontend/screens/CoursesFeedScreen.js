import {Text, View} from "react-native";

export default function CoursesFeedScreen({navigation}) {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Courses Feed!</Text>
        </View>
    );
}